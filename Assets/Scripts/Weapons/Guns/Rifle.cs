using System.Collections;
using UnityEngine;

public class Rifle : Gun
{
    // No idea if this will be necessary. The only use I can think of so far is to 
    // have different default values from the Gun class, which can just be set in editor instead.
}
